# Example only. Rename this file to backend.tf and replace the placeholders
# before using it in a shared environment. The S3 bucket and DynamoDB table
# must already exist.
#
# terraform {
#   backend "s3" {
#     bucket         = "YOUR_TERRAFORM_STATE_BUCKET"
#     key            = "three-tier/dev/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "YOUR_TERRAFORM_LOCK_TABLE"
#   }
# }
